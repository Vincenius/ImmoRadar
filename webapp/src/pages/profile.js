import Layout from '@/components/Layout/Layout';

const Profile = () => {
    // todo show notification if confirm query param is true and remove it from the URL
    return (
        <Layout>
            <h1>Profile Page</h1>
            {/* todo show user notification settings */}
        </Layout>
    );
};

export default Profile;